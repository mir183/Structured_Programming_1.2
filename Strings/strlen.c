#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Programming";

    // Using strlen for string length
    int length = strlen(str);

    // Printing the result
    printf("strlen Result: %d\n", length);

    return 0;
}
